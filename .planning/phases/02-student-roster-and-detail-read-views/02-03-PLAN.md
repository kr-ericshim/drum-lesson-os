---
phase: 2
plan: 02-03
title: Recent note ordering and read-view verification
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/lib/supabase/queries.ts
  - src/components/students/student-notes-list.tsx
  - src/components/students/student-detail-tabs.tsx
  - README.md
autonomous: true
requirements: [STUD-02, NOTE-03]
requirements_addressed: [STUD-02, NOTE-03]
---

<objective>
As an instructor, I want recent lesson notes to appear in newest-first order on the student detail page, so I can quickly remember the latest teaching context before a lesson.
</objective>

<context>
Read before executing:
- `.planning/phases/02-student-roster-and-detail-read-views/02-CONTEXT.md`
- `.planning/phases/02-student-roster-and-detail-read-views/02-UI-SPEC.md`
- `.planning/phases/02-student-roster-and-detail-read-views/02-RESEARCH.md`
- `.planning/phases/02-student-roster-and-detail-read-views/02-02-PLAN.md`
- `AGENTS.md`
</context>

## truths

- D-07: Show the most recent 3 lesson notes.
- D-08: Recent notes sort by `lesson_date` descending.
- D-09: Each note shows `covered_material`, `observations`, `practice_assigned`, and `next_step_hint`.
- D-10/D-12: Empty and error states stay quiet and useful.

<must_haves>
- NOTE-03 is covered: recent notes appear in reverse chronological order.
- STUD-02 is completed with recent lesson notes included alongside progress, traits, weak points, assignment status, and next lesson plan.
- The notes UI remains read-only and compact.
- Verification proves all Phase 2 requirement IDs are covered by plan/source evidence.
</must_haves>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Notes show old context first | MEDIUM | Enforce `lesson_date` descending order in the query or deterministic mapper and verify with source assertion. |
| Notes tab renders too much free text and becomes unscannable | LOW | Limit to 3 notes and use compact labeled fields. |
| Verification misses a dropped Phase 2 requirement | MEDIUM | Run requirement coverage checks against plan frontmatter and source assertions. |
</threat_model>

<tasks>
<task id="02-03-T1" type="execute">
  <title>Enforce recent note limit and ordering in the detail query</title>
  <read_first>
    <file>src/lib/supabase/queries.ts</file>
    <file>supabase/migrations/0001_foundation.sql</file>
    <file>.planning/phases/02-student-roster-and-detail-read-views/02-RESEARCH.md</file>
  </read_first>
  <action>
    Update `getStudentDetail` so `lesson_notes` are ordered by `lesson_date` descending and limited to 3 visible notes. Use a Supabase nested relation ordering if practical; otherwise sort and slice deterministically in the mapper using `lesson_date`. Preserve all note fields: `lesson_date`, `covered_material`, `observations`, `practice_assigned`, and `next_step_hint`.
  </action>
  <acceptance_criteria>
    <criterion>`src/lib/supabase/queries.ts` includes `lesson_date` in the lesson note read model.</criterion>
    <criterion>`getStudentDetail` includes either `.order("lesson_date", { ascending: false` or a deterministic descending `lesson_date` sort.</criterion>
    <criterion>`getStudentDetail` limits visible notes to 3 using `.limit(3` or `.slice(0, 3)`.</criterion>
    <criterion>The note mapper preserves `covered_material`, `observations`, `practice_assigned`, and `next_step_hint`.</criterion>
  </acceptance_criteria>
</task>

<task id="02-03-T2" type="execute">
  <title>Render compact recent notes in the Notes tab</title>
  <read_first>
    <file>src/components/students/student-detail-tabs.tsx</file>
    <file>src/components/students/student-notes-list.tsx</file>
    <file>src/components/ui/card.tsx</file>
    <file>src/components/ui/separator.tsx</file>
    <file>.planning/phases/02-student-roster-and-detail-read-views/02-UI-SPEC.md</file>
  </read_first>
  <action>
    Create `src/components/students/student-notes-list.tsx` and wire it into the `Notes` tab. Render up to 3 notes with compact labels for `Covered`, `Observations`, `Practice`, and `Next hint`. Include an empty state such as `No lesson notes yet` when the notes array is empty. Keep note cards/panels 8px radius or less and avoid oversized editorial styling.
  </action>
  <acceptance_criteria>
    <criterion>`src/components/students/student-notes-list.tsx` exists.</criterion>
    <criterion>The notes component renders `Covered`, `Observations`, `Practice`, and `Next hint` labels.</criterion>
    <criterion>`student-detail-tabs.tsx` renders `StudentNotesList` inside the `Notes` tab.</criterion>
    <criterion>The notes empty state includes `No lesson notes yet`.</criterion>
    <criterion>No file under `src/components/students` contains `<textarea` or `<input` for notes.</criterion>
  </acceptance_criteria>
</task>

<task id="02-03-T3" type="execute">
  <title>Add final read-view verification and documentation</title>
  <read_first>
    <file>README.md</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/phases/02-student-roster-and-detail-read-views/02-CONTEXT.md</file>
  </read_first>
  <action>
    Add a concise Phase 2 manual verification note to `README.md`: dashboard lists active students, row opens detail, summary/progress/notes tabs render, and notes are newest-first. During execution, run final build/lint/source checks and record results in the plan summary.
  </action>
  <acceptance_criteria>
    <criterion>`README.md` mentions `Summary`, `Progress`, and `Notes` as read-only detail tabs.</criterion>
    <criterion>`README.md` mentions notes are newest-first or ordered by latest lesson date.</criterion>
    <criterion>`npm run build` exits 0.</criterion>
    <criterion>`npm run lint` exits 0.</criterion>
  </acceptance_criteria>
</task>
</tasks>

<verification>
- Run `npm run build`.
- Run `npm run lint`.
- Run `rg "lesson_date|slice\\(0, 3\\)|limit\\(3|Covered|Observations|Practice|Next hint" src/lib src/components/students`.
- Run `rg "ROST-01|STUD-01|STUD-02|NOTE-03" .planning/phases/02-student-roster-and-detail-read-views/*-PLAN.md`.
- Manually load `/` and one `/students/{id}` route when Supabase seed data is configured; if Supabase CLI/env is unavailable, record that manual seeded-data route verification remains pending.
</verification>

<success_criteria>
- Recent notes are limited to 3 and newest-first.
- Student detail shows progress, recent notes, traits, weak points, assignment status, and next lesson plan together across the tabbed read view.
- Phase 2 plans cover ROST-01, STUD-01, STUD-02, and NOTE-03.
- No edit workflow is introduced.
</success_criteria>
