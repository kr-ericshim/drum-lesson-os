---
phase: 2
plan: 02-01
title: Roster data loader and clickable student list
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/queries.ts
  - src/app/page.tsx
  - src/components/dashboard/student-roster-preview.tsx
  - src/components/dashboard/student-summary-row.tsx
  - README.md
autonomous: true
requirements: [ROST-01]
requirements_addressed: [ROST-01]
---

<objective>
As an instructor, I want the dashboard roster to show all active students and let me open one student, so the Phase 1 preview becomes the start of the real read workflow.
</objective>

<context>
Read before executing:
- `.planning/phases/02-student-roster-and-detail-read-views/02-CONTEXT.md`
- `.planning/phases/02-student-roster-and-detail-read-views/02-UI-SPEC.md`
- `.planning/phases/02-student-roster-and-detail-read-views/02-RESEARCH.md`
- `.planning/phases/01-app-foundation-and-data-model/01-03-SUMMARY.md`
- `AGENTS.md`
</context>

## truths

- D-01: Keep and extend the Phase 1 roster row structure.
- D-02: Rows keep name, profile cue, current focus, primary weak point, assignment status, and next lesson action.
- D-03: Roster stays compact and workbench-like.
- D-10/D-11: Empty, setup, and query failure states stay readable and distinct.

<must_haves>
- ROST-01 is covered: dashboard lists all active students returned by the Supabase query.
- Roster rows can open the corresponding student detail route via `/students/{id}`.
- The dashboard does not introduce search, filtering, editing, scheduling, billing, or student portal UI.
- Missing Supabase env still renders setup-aware empty state without calling Supabase.
</must_haves>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Roster query accidentally exposes inactive or cross-instructor students | HIGH | Keep `.eq("active", true)` and rely on existing RLS/instructor ownership; do not add service-role clients. |
| Clickable rows become inaccessible to keyboard or screen readers | MEDIUM | Use a real `Link`/anchor or an explicit accessible open action with visible focus state. |
| Roster scope creeps into editing | MEDIUM | Do not add forms, mutations, or create/edit CTAs in Phase 2 roster work. |
</threat_model>

<tasks>
<task id="02-01-T1" type="execute">
  <title>Rename and expand the roster query contract</title>
  <read_first>
    <file>src/lib/supabase/queries.ts</file>
    <file>src/types/database.ts</file>
    <file>supabase/migrations/0001_foundation.sql</file>
    <file>.planning/phases/02-student-roster-and-detail-read-views/02-RESEARCH.md</file>
  </read_first>
  <action>
    In `src/lib/supabase/queries.ts`, introduce a roster-oriented DTO named `StudentRosterItem` and a query named `getStudentRoster`. Preserve the existing fields `id`, `name`, `profileCue`, `currentFocus`, `weakPoint`, `assignmentStatus`, and `nextAction`. The query must read from `students`, include assignment and next lesson relations, filter `active = true`, and order students by `name` ascending. Keep or alias `getStudentDashboardPreview` only if needed for backwards compatibility in the same file.
  </action>
  <acceptance_criteria>
    <criterion>`src/lib/supabase/queries.ts` exports `type StudentRosterItem`.</criterion>
    <criterion>`src/lib/supabase/queries.ts` exports `getStudentRoster`.</criterion>
    <criterion>The roster query contains `.eq("active", true)`.</criterion>
    <criterion>The roster query orders by `name` ascending.</criterion>
    <criterion>`SERVICE_ROLE` does not appear in `src/lib/supabase/queries.ts`.</criterion>
  </acceptance_criteria>
</task>

<task id="02-01-T2" type="execute">
  <title>Wire the dashboard to the roster query</title>
  <read_first>
    <file>src/app/page.tsx</file>
    <file>src/lib/env.ts</file>
    <file>src/lib/supabase/queries.ts</file>
    <file>src/components/dashboard/student-roster-preview.tsx</file>
  </read_first>
  <action>
    Update `src/app/page.tsx` to call `getStudentRoster` when Supabase setup state is `configured`. Keep the missing-env path returning an empty list with no query. Update variable names and props so the page reads as a real roster screen, while preserving `Drum Lesson OS` and the instructor workbench layout.
  </action>
  <acceptance_criteria>
    <criterion>`src/app/page.tsx` imports `getStudentRoster`.</criterion>
    <criterion>`src/app/page.tsx` still checks `setupStatus.state === "configured"` before calling the Supabase query.</criterion>
    <criterion>The page renders the copy `Student roster` or passes that heading to the roster component.</criterion>
    <criterion>`npm run build` exits 0 after this phase completes.</criterion>
  </acceptance_criteria>
</task>

<task id="02-01-T3" type="execute">
  <title>Make roster rows open student details</title>
  <read_first>
    <file>src/components/dashboard/student-roster-preview.tsx</file>
    <file>src/components/dashboard/student-summary-row.tsx</file>
    <file>src/components/ui/button.tsx</file>
    <file>.planning/phases/02-student-roster-and-detail-read-views/02-UI-SPEC.md</file>
  </read_first>
  <action>
    Update the roster components so each student item has a clear route to `/students/{student.id}`. Prefer a real `Link` wrapping the row or a single explicit `Open student` action. Preserve row content order: name/profile cue, current focus, weak point, assignment badge, next lesson. Add focus-visible styling that uses the existing ring/primary token.
  </action>
  <acceptance_criteria>
    <criterion>`src/components/dashboard/student-summary-row.tsx` imports `Link` from `next/link` or renders an anchor-compatible `Button asChild` to `/students/${student.id}`.</criterion>
    <criterion>The visible or accessible action text includes `Open student`.</criterion>
    <criterion>Roster item content still renders `Current focus`, `Weak point`, `Assignment`, and `Next lesson` labels.</criterion>
    <criterion>No file in `src/components/dashboard` contains `<form`.</criterion>
  </acceptance_criteria>
</task>

<task id="02-01-T4" type="execute">
  <title>Document roster read-view smoke check</title>
  <read_first>
    <file>README.md</file>
    <file>.planning/phases/02-student-roster-and-detail-read-views/02-CONTEXT.md</file>
  </read_first>
  <action>
    Add a short Phase 2 roster smoke-check note to `README.md`: with Supabase env and seed data configured, the dashboard should show active students and each row should link to `/students/{id}`. Keep setup wording concise.
  </action>
  <acceptance_criteria>
    <criterion>`README.md` mentions `/students/{id}` or `/students/[studentId]`.</criterion>
    <criterion>`README.md` does not mention editing students as a Phase 2 capability.</criterion>
  </acceptance_criteria>
</task>
</tasks>

<verification>
- Run `npm run build`.
- Run `npm run lint`.
- Run `rg "getStudentRoster|StudentRosterItem|Open student|/students" src README.md`.
- Run `rg "<form|insert\\(|update\\(|delete\\(" src/components/dashboard src/app/page.tsx src/lib/supabase/queries.ts` and confirm no Phase 2 roster write workflow was introduced.
</verification>

<success_criteria>
- Active students render in the dashboard roster.
- Each roster row can open the intended detail route.
- Setup/empty/error states remain non-crashing and instructor-toned.
- No editing workflow is introduced.
</success_criteria>
